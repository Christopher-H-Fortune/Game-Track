<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fullsail.christopherfortune.gametrack">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".LoginSignUpActivity.LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GamesListActivity.GamesListActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".AddGameActivity.AddGameActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".MatchListActivity.MatchListActivity" />
        <activity android:name=".AddMatchActivity.AddMatchActivity" />
        <activity android:name=".SelectedMatchActivity.SelectedMatchActivity" />
        <activity android:name=".CompareMatchesActivity.CompareMatchesActivity"/>
    </application>

</manifest>